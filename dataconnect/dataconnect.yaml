specVersion: "v1beta"
serviceId: "edu-track"
location: "asia-east1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "postgres"
      cloudSql:
        instanceId: "edutrack-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
